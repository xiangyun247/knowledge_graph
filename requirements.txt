fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.5.2,<3.0.0  # langchain-core 0.3.x 要求 >=2.5.2；与 FastAPI/Chromadb 兼容
pydantic-settings==2.1.0
neo4j==5.14.1
openai>=1.58.1,<2.0.0  # langchain-openai 0.2.x 要求 >=1.58.1；DeepSeek 兼容 OpenAI SDK
sentence-transformers==2.2.2  # 本地 embedding
torch>=2.0.0  # sentence-transformers 依赖
python-multipart==0.0.6  # 文件上传
python-dotenv==1.0.0
pdfplumber==0.10.3
pymupdf>=1.23.0  # PDF 页渲染为图像，供扫描版 OCR 使用
mysql-connector-python==8.2.0  # MySQL数据库连接
sqlalchemy==2.0.23  # SQL ORM框架
pymysql==1.1.2
numpy<2  # 与部分依赖兼容；chromadb 0.5.x 可配合 pydantic 2
chromadb>=0.5.0,<0.6.0  # 文档知识库向量存储（0.5.x 支持 pydantic 2）
# LangGraph Agent
langgraph>=0.2.0,<0.3
langchain-core>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3  # 兼容 DeepSeek 的 OpenAI 接口
loguru>=0.7.0  # 日志（backend.app）
# 多模态：TTS、STT、OCR
edge-tts>=6.1.0  # 文本转语音
openai-whisper>=20231117  # 语音转文本
paddlepaddle>=2.5.0  # OCR（CPU 版；GPU 需单独安装）
paddleocr>=2.7.0  # 光学字符识别
PyJWT>=2.8.0
bcrypt>=4.0.0
slowapi>=0.1.9
# EEG 硬件支持 (Muse S / Lab Streaming Layer)
pylsl>=1.16.0  # LSL 协议，用于连接 EEG 硬件
scipy>=1.11.0  # 信号处理（频段功率计算）
mne>=1.5.0  # EEG 数据读取（支持 EDF 等格式）
# 测试
pytest>=7.0.0
pytest-cov>=4.0.0